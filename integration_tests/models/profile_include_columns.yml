version: 2

models:
  - name: profile_include_columns
    columns:
      - name: column_name
        tests:
          - accepted_values:
              arguments:
                values: ["numeric_not_nullable"]